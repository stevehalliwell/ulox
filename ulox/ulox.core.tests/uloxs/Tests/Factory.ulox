class Foo
{
    var a = 1;
}

register Foo fun (){ retval = Foo(); };

test FactoryTests
{
    testcase OutterFactory
    {
        var foo = inject Foo ();

        Assert.AreEqual(1, foo.a);
    }
}
