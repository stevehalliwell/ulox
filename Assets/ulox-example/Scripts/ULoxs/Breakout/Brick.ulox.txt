class Brick
{
    var pos = Vec2();
    var go = CreateFromPrefab("Brick");

    init(pos)
    {
        this.SyncPos();
    }

    SyncPos()
    {
        SetGameObjectPosition(this.go, this.pos.x, this.pos.y, 0);
    }
}