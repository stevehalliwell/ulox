// system is shorthand for static classes with no init or data
//  only static functions. 
// Systems can be mixin other systems.
// Often a System will have matching data class that is given as the context for it to operate on.

system Foo 
{
    Bar()
    {
        retval = "hello world";
    }
}

print (Foo.Bar());
