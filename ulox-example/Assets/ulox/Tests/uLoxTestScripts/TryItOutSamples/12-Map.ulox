// standard classes library provides a map type, 
//  a key value pair look up type with CRUD
build bind "StandardClassesLibrary";

var key = "key";
var expected = "value";
var m = Map(key);

m.Create(key, expected);
var result = m.Read(key);
m.Update(key, expected);
m.Delete(key);
