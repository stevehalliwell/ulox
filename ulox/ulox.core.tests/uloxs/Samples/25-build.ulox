//build allows you to enqueue and execute scripts
// we're going to build a known other script but we could also build and run everything in this folder via the platform

var files = Platform.FindFiles("./uloxs/", "01-Hello.ulox", true);

loop files
{
    Build.QueueFile(item);
    //including double build prevention
    Build.QueueFile(item);
}

Build.ProcessQueue();
// calling this at the tail end of a vm means we end up in bad place during it's compilation. We need to be able to nested compile or defer the build to a safe time.